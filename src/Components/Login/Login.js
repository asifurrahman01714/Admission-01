import React from 'react';
import Header from '../Header/Header';

function Login(props) {
    return (
        <div>
            <Header/>
            <h1>Login</h1>
        </div>
    );
}

export default Login;   